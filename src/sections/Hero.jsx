import React from 'react';

import '../styles/hero.css';

import NavbarComponent from '../components/NavbarComponent';

const Hero = () => {
    return (
        <div className="hero">
            <NavbarComponent />
        </div>
    );
};

export default Hero;
